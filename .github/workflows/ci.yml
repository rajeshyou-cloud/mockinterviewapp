name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  web:
    name: Web tests and build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm
      - run: npm install
      - run: npm run test:web
      - run: npm run test:db-schema
      - run: npm run test:content-import
      - run: npm run test:review-runtime
      - run: npm run import:governed-content -- --dry-run
      - run: npm run validate:benchmarks
      - run: npm run check:evidence-links -- --offline --fail-on-broken
      - run: npm run report:content-scale -- --no-write
      - run: npm run build:web

  api:
    name: API tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/api
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: apps/api/requirements.txt
      - run: pip install -r requirements.txt
      - run: python -m pytest -q
